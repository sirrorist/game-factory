# Состояние проекта

> Переписывается в конце каждой сессии. Каждое утверждение — с источником:
> **проверено** (команда и итог) / **со слов владельца** / **предположение**.

Обновлено: 2026-09-25, сессия №2 (этап 0б).

## Где мы

Этап **0а** — принят владельцем (2026-09-25).
Этап **0б** — все пункты сделаны и проверены (ниже), **ждёт приёмки владельца**.
Приёмка — на проде: владелец решил разворачиваться сейчас за своим Traefik (D-033).
**Следующая сессия — на VPS**, по спецификации [`specs/deploy-vps.md`](specs/deploy-vps.md).
В заголовке ROADMAP этап всё ещё помечен ⛔ «нужен доступ к registry.npmjs.org»:
реестр открыт, но снять блокер и сменить статус — решение владельца (правило кита).

Код сессии №2 — коммит `cbf3732` в `main` (владелец дал «да»; D-030). Хэндовер со
спецификацией деплоя — вторым коммитом, только документация (CI не запускает, D-031).

## Что сделано в сессии №2

- `pnpm-lock.yaml` создан (`pnpm install`, реестр открыт); CI ставит зависимости
  с `--frozen-lockfile`, шаг-предупреждение про lockfile убран.
- `packages/vite-config` — общий Vite-конфиг; офлайн-режим собирает игру в один
  `index.html` с классическим скриптом (IIFE) в конце `<body>` (D-021).
- `templates/vite-ts` — шаблон игры; `gf new <id>` копирует его в `games/<id>` (D-025).
- Демо `phaser-2d` «Звездопад» (Phaser 4.2.1) и `three-3d` «Три полосы» (Three 0.186.1),
  обе `offline: true`, текстуры и геометрия — кодом (D-029).
- `apps/hub` — Next.js 16.3.6, React 19.3, Tailwind 4, компоненты в стиле shadcn (D-022):
  каталог из `registry.json` с обложками, страница игры с iframe и мостом, рекорд
  игрока в интерфейсе хаба, «Скачать» для офлайн-игр, «На весь экран» при праве `fullscreen`.
- `frameAttributes` в `hub-bridge`: права манифеста → `sandbox` и `allow` iframe (D-024).
- Защитные заголовки хаба: `frame-ancestors 'none'` и др. (D-028).
- `Dockerfile` (цели `hub`, `play`) + `docker-compose.yml` с ужесточением контейнеров (D-027);
  в CI — задача `docker` (`docker compose build`).
- e2e: `tests/e2e/hub.test.ts` на настоящем хабе, офлайн-тесты Phaser и Three.
- PR Dependabot поглощены коммитом сессии (D-032): TypeScript 7.0.2 везде, Playwright 1.63.0,
  Actions checkout 7 / setup-node 7 / upload-artifact 7 / pnpm/action-setup 6.
- Экономия CI (D-031): прогона нет на правки только документации; Docker — отдельный
  `docker.yml` по изменению Docker-файлов и lockfile; Dependabot раз в месяц пачками.
- `apps/hub/AGENTS.md` + `CLAUDE.md` от `next dev` — в репозитории (владелец: «ок», П-029).
- Иконка хаба (`icon.svg`) — без неё полный Chromium ловил 404 (П-031).
- Документы: ARCHITECTURE, DECISIONS (D-021…D-033), PITFALLS (П-018…П-031), GAMES,
  SDK, SECURITY, TESTING, DEVELOPMENT, DEPLOY, README, ROADMAP (пункты 0б → 🔎).
- Спецификация деплоя на VPS: `docs/specs/deploy-vps.md` (D-033).

## Что проверено и чем

| Что | Как | Итог |
|---|---|---|
| Реестр npm | `curl … https://registry.npmjs.org/zod` | 200 |
| Исходное состояние | `pnpm install && pnpm check` до правок | 37 / 37 юнит, 6 / 6 e2e — совпало со STATE сессии №1 |
| CI `main` до правок | GitHub API, запуск #10 на `9401e67` | зелёный |
| Полный прогон | `pnpm check` с чистого состояния (без `.data`, `.next`, `dist`) | exit 0: **44 / 44 юнит, 20 / 20 e2e**, ~50 с |
| То же на TS 7.0.2 + Playwright 1.63.0 | `GF_CHROMIUM_PATH=/opt/pw-browsers/chromium pnpm check` с чистого состояния | exit 0: 44 / 44, 20 / 20 |
| TS 7 действительно проверяет | мутации: `enum` и `string` в `number` в ядре; ошибка типа в хабе → `next build` | ядро: TS1294 + TS2322; хаб: «Failed to type check» |
| Образы на новых версиях | `docker build` целей `hub` и `play` (зеркало + CA) | собрались |
| Типы | `pnpm typecheck`: ядро, SDK, 2 игры, шаблон, хаб (`next typegen && tsc`) | 0 ошибок |
| Детерминированность сборок Vite | `pnpm games:build` дважды | «без изменений» у всех трёх игр |
| Мутации защит 0б | 4 мутации (TESTING.md) | все 4 теста упали; файлы сверены `cmp`; контроль 15 / 15 |
| Хаб вручную | `pnpm play` + `pnpm hub:start`, `curl`, скриншоты Playwright | каталог из 3 игр, игра в iframe, 404 на `/games/nope` |
| Dev-режимы | `pnpm --filter @gf-game/phaser-2d dev`, `next dev` | Vite отдаёт `main.ts`; `next dev` — 200 на `/` и `/games/snake` |
| CI на `cbf3732` | GitHub API: workflow `CI` и `Docker` | оба `success` |
| PR Dependabot | GitHub API после пуша | #1, #2 закрыл Dependabot; #3, #4, #6, #7 закрыты агентом с комментарием; открытых PR — 0 |
| Репозиторий публичный | `curl` к API GitHub без авторизации отвечает | да — важно для выбора доставки (spec, вариант В отвергнут) |
| Docker | `dockerd &`, `docker build` хаба (зеркало + CA, П-025…П-027), `docker compose up --no-build` | play healthy, хаб 200, 3 игры в каталоге, архив 200, игра в хабе ready (Playwright); контейнер под `node`, ФС только для чтения |

## Что НЕ проверено

- `docker compose build` целиком на обычной машине: здесь Docker Hub дал 429, а build-secret
  с CA compose не передаёт (П-026, П-027); образ `play` собран через compose, `hub` — `docker build`.
- Браузеры кроме Chromium, телефон, реальные домены и HTTPS.
- WebGL только программный (SwiftShader) — на настоящей видеокарте игры не запускались.

## Блокеры

Нет. (Прежний блокер «реестр npm закрыт» устранён по факту, снять его в ROADMAP — за владельцем.)

## Следующий шаг

1. **Сессия на VPS** (владелец запускает её сам): деплой по `docs/specs/deploy-vps.md` —
   разведка, образ `games`, `compose.prod.yml`, маршруты Traefik, публикация образов в GHCR,
   таймер обновления, критерии приёмки. Команды, меняющие сервер, — блоками для владельца.
2. Владелец принимает этап 0б на `https://games.youranus.ru` и решает про ⛔ в ROADMAP.
3. Возврат в облачную сессию: сквозная задача «роли агентов» (ROLES.md) или этап 1.

## Открытые вопросы к владельцу

- Приёмка этапа 0б и снятие блокера в ROADMAP — после деплоя.
- Есть ли у Traefik сертификат именно на `*.play.youranus.ru` (DNS-01)? `*.youranus.ru`
  его не покрывает (П-004) — выяснит разведка сессии на VPS.
- Образы в GHCR публичные (как репозиторий) — годится? Иначе нужен токен `read:packages` на VPS.

## Хэндовер: новая сессия

1. Первая строка ответа — канарейка (правило кита), нумерация ходов с 1.
2. Прочитать `CLAUDE.md`, этот файл, `docs/ROADMAP.md`, `docs/PITFALLS.md`.
3. **Сессия на VPS:** дальше — `docs/specs/deploy-vps.md`, начиная с «Разведки».
   На VPS нет `/opt/pw-browsers` — `GF_CHROMIUM_PATH` не нужен, браузер ставит
   `pnpm exec playwright install chromium`, если там вообще гонять e2e.
4. **Облачная сессия:** `pnpm install && GF_CHROMIUM_PATH=/opt/pw-browsers/chromium pnpm check`
   — должно совпасть с таблицей выше (44 юнит, 20 e2e). Playwright в lockfile — 1.63.0,
   браузер облачной среды — `chromium-1194` (1.56), отсюда переменная (П-030).
5. Проверить CI последнего коммита в `main` и отчёт сессии на VPS в этом файле.

Контекст решений, который легко потерять:
- Владелец доверил выбор движков агенту (D-005); хаб — Next.js, сборка — Vite (владелец).
- Домен для игр сейчас `*.play.youranus.ru`; новый домен владелец купит перед открытием (D-010).
- Деплой на VPS — делается сейчас за Traefik владельца (D-033, заменяет D-017 в части деплоя).
  Роли агентов — план (D-020, ROLES.md).
- Репозиторий публичный: self-hosted runner на VPS нельзя (чужие PR выполнят код на сервере).
- Офлайн-архив — только для статических игр; серверные — этап 3 (D-012).
- Хаб на этапе 0 — гость без авторизации, данные игр в `localStorage` хаба (D-024).

## Для следующей сессии

- MVP: коммит прямо в `main`, один на сессию, с «да» владельца (D-030). Ветку
  `claude/…`, которую заводит облачная среда, не используем.
- Минуты CI беречь (D-031): не пушить по мелочи, проверять локально.
- Docker в облачной среде: `dockerd &`, образ — через `mirror.gcr.io`, хаб — `docker build`
  с `--secret id=extra_ca,src=/root/.ccr/ca-bundle.crt` (П-025…П-027).
