# Состояние проекта

> Переписывается в конце каждой сессии. Каждое утверждение - с источником:
> **проверено** (команда и итог) / **со слов владельца** / **предположение**.

Обновлено: 2026-09-26, сессия №5 (облако: приёмка 0б и деплоя, `check-prod`, план) - хэндовер.
Сессия №4 (облако) шла параллельно с №5; её итоги - ниже, отдельным разделом. Прод и VPS - по итогам сессии №3.

## Где мы

Этапы **0а** и **0б** - приняты владельцем (0б - 2026-09-26, "Да, принимаю"; блокер снят).
Деплой на VPS-1 принят (владелец: "прими так"; критерий 9 - со слов владельца, без вывода).
Прод работает: `https://games.youranus.ru`, игры на `*.play.youranus.ru`. **Работа - в `dev`**
(D-042), `main` = прод; выкладка - перемотка `dev` в `main` с отдельным "да".
В `dev` и ещё не на проде: кеш обложек (D-041), `check-prod` (D-044), тест ссылок документов.
Дальше (D-045): роль агента "Безопасность", затем этап 1.

## Что сделано в сессии №5 (облако)

- ROADMAP: этап 0б ✅ и деплой ✅ (владелец); "Слабые места" - 🔎 у сделанных, ждут приёмки.
- Старые коммиты на GitHub по прямому хешу - оставить (владелец: "не надо, пусть лежат"; D-043).
- **`gf check-prod` (D-044)** + шаг CI после `games:build`: та же version с другим содержимым,
  чем на проде, - красный CI "подними version" (П-034). Прод недоступен - предупреждение.
  Метка содержимого - `contentTag` в `packages/registry`.
- 304 по `If-None-Match` - в `streamFile`: теперь и у архивов (было у игр и обложки).
- `tools/docs.test.ts`: ссылки `D-`/`П-` ведут на существующие записи, номера не повторяются.
- Правило в `CLAUDE.md` ("Конец сессии"): "проверено" - только с командой и итогом.
- Сессия №5 сначала сделала свой кеш обложек (метка-хеш в адресе, `preconnect`) параллельно
  с №4; при переносе на `dev` оставлена реализация №4 (D-041), своя отброшена: D-041 прямо
  отвергает `preconnect`, хеш вместо версии там же разобран.
- Документы: DECISIONS D-044, D-045; PITFALLS П-034 (дополнен), П-040; TESTING, README, ROADMAP.

## Что сделано в сессии №4 (облако, параллельно с №5)

- Обложки с долгим кешем (D-041): хаб просит `/covers/<id>?v=<version>`, сервер игр отдаёт
  `immutable` на год только для текущей версии; без версии или со старой - `no-cache` + `ETag`/304.
  Файлы: `apps/play-server/src/server.ts`, `apps/hub/src/lib/catalog.ts`, тест в
  `apps/play-server/test/server.test.ts`, проверка адреса в `tests/e2e/hub.test.ts`.
- Документы: SECURITY (строка про кеш обложки), TESTING (счёт тестов: `gf.test.ts` 8, не 7;
  всего 46 юнит), ROADMAP (факт у пункта, **статус не менялся**), D-041, П-039.
- В клоне облака был `main` до переписи истории (П-039): работа велась от `origin/main`
  (`a26758d`) в detached HEAD, локальные ветки не трогались.
- Ветка `dev` (D-042, владелец: "давай добавим ветку dev"): создана от `a26758d`, коммит
  сессии - в неё; CI и Docker гоняются на `dev` и `main`, Publish (прод) - только с `main`;
  Dependabot - в `dev`. Процесс записан в CLAUDE.md, OPERATIONS, DEVELOPMENT, DEPLOY.
  Правило владельца: новые ветки - только с его разрешения.
- Старые локальные ветки облачного клона (`main` и `claude/…` со старой историей, П-039)
  удалены по слову владельца; устаревшие ссылки на удалённые ветки - `git fetch --prune`.
- Документы вычищены от деталей сервера (владелец: "проверь ВСЕ файлы документов, чтобы
  там не было информации о сервере"); что считается такой информацией - D-043.
- Чистка закоммичена в `dev` (`267e631`) и к сессии №5 уже была на GitHub
  (`git ls-remote origin` - проверено).

## Сессия №3 (VPS-1) - кратко

Деплой по спецификации, изоляция `gf-edge` (D-038), репозиторий публичный (D-037), история
переписана (D-039), "Звездопад" 1.1.0 (D-040), ручной запуск CI (П-038). Подробности - в
DECISIONS D-034…D-040 и PITFALLS П-032…П-038; проверки - в таблицах ниже.

## Что проверено и чем

### Сессия №5 (облако, 2026-09-26)

| Что | Как | Итог |
|---|---|---|
| Исходное состояние | `pnpm install && GF_CHROMIUM_PATH=/opt/pw-browsers/chromium pnpm check` на `a26758d` | 45 юнит, 20 e2e - зелёное |
| Сборка воспроизводима облако ↔ прод | `curl -sI https://<id>.play.youranus.ru/` (ETag) против хешей `.data/registry.json` | все три игры совпали |
| `gf check-prod` на настоящем проде | `GF_PROD_GAME_URL='https://{id}.play.youranus.ru/' node tools/gf.ts check-prod` | 3 × "совпадает с продом", exit 0 |
| `check-prod` - тест не пустой | `gf.test.ts` против поддельного прода; мутация "метку не сравнивать" | краснеет (8/9) |
| 304 в `streamFile` | мутация "проверка `If-None-Match` убрана" | краснеют 3 теста `server.test.ts` |
| Тест ссылок | до записи D-044 в DECISIONS | краснел на `packages/registry/src/index.ts: D-044` |
| Итог на `dev` + правки | `GF_CHROMIUM_PATH=/opt/pw-browsers/chromium pnpm check` | **49 юнит, 20 e2e** - зелёное |

### Сессия №4 (облако, 2026-09-26)

| Что | Как | Итог |
|---|---|---|
| Исходное состояние | `pnpm install && GF_CHROMIUM_PATH=/opt/pw-browsers/chromium pnpm check` на `a26758d` | 45 юнит, 20 e2e - зелёное, совпадает с хэндовером |
| После правки обложек | тот же `pnpm check` | 46 юнит, 20 e2e - зелёное |
| Тест кеша обложки - не пустой | мутация "immutable всегда" и мутация "без 304" | каждая - 1 fail из 15 в `server.test.ts`; код возвращён, 15/15 |
| Перед коммитом в `dev` (после "да" владельца) | `pnpm check` с чистого состояния (без `.data`, `.next`, `dist`); повтор мутации "immutable всегда" | 46 / 46 юнит, 20 / 20 e2e; мутация - `not ok`, файл сверен `cmp` |

### Сессия №3 (VPS-1)


| Что | Как | Итог |
|---|---|---|
| Архитектура VPS-1 | `uname -m` | x86_64 → образы `linux/amd64` |
| Traefik | конфиг Traefik владельца | provider docker; резолверы HTTP-01 и DNS-01 уже есть; сверху сжатие и HSTS без `includeSubDomains` |
| Веб-адрес | `curl --resolve … 2.26.198.231` | Traefik отвечает на `2.26.198.231` |
| Репозиторий | `gh api repos/sirrorist/game-factory` | был `private`, стал `public` |
| DNS | `dig`: `games`, `play`, `snake.play`, `xyz.play` | все → `2.26.198.231` |
| Ключ развёртывания | `ssh -T` к GitHub | аутентификация для `sirrorist/game-factory` успешна |
| Секреты в истории | `git log --all -p` + шаблоны токенов/ключей, адресов, почты | ничего |
| `gf build --prebuilt` | `node --test tools/gf.test.ts` | 8 / 8 |
| Публикатор `games` без Docker | набор файлов как в образе, `gf build --prebuilt && gf export` дважды, затем правка без смены version | "опубликована" → "без изменений" → "неизменяема", exit 1 |
| `update.sh` | `shellcheck`, `bash -n` | чисто |
| `compose.prod.yml` | `docker compose config` | разбирается |
| Агент и Docker | `id` | пользователь агента не в группе `docker` (П-036) |

### Выкладка и приёмка на проде (2026-09-25)

| Что | Как | Итог |
|---|---|---|
| CI, Docker, Publish на первом коммите деплоя | `gh run list` | все `success` |
| Образы GHCR без токена | анонимный токен GHCR + манифест `:main` | 200 у `hub`, `play`, `games` |
| Первая выкладка (владелец) | `gf-update --force` | 3 игры "опубликована", 3 архива; `play` healthy, `hub` Up |
| Таймер | `systemctl list-timers`, `systemctl status gf-update.service` | следующий запуск через 5 мин; прогон по таймеру `status=0/SUCCESS` |
| 1. Каталог | `curl https://games.youranus.ru/` | Змейка, Звездопад, Три полосы |
| 3. Архивы | ссылки из хаба, `curl` | `play.youranus.ru/exports/<id>-1.0.0.zip`, 200 |
| 4. Заголовки хаба | `curl -sI` | `frame-ancestors 'none'`, `nosniff`, HSTS 30 дней, `X-Powered-By` нет |
| 5. Служебный хост | `curl` | `/index.html` 404, `/registry.json` 404, `/healthz` 200 |
| 7. Порты | `ss -tlnH` | порты хаба и сервера игр на хосте не опубликованы |
| 6. Сертификат `x.play` | `openssl s_client -servername snake.play…` | ✅ `*.play.youranus.ru` + `play.youranus.ru`, Let's Encrypt YR2, до 2026-12-24; выпуск занял ~55 мин (П-037) |

### Изоляция прода (2026-09-26, D-038)

| Что | Как | Итог |
|---|---|---|
| Хаб и сервер игр в `gf-edge` | зонд `node net.connect` в контейнере (владелец) | до хоста, чужих контейнеров и интернета не достают; игра 200 |
| Веб-адрес | с телефона через мобильную сеть | хаб открывается, игра работает |

### Критерии приёмки, дополнительно (2026-09-26)

| Критерий | Как | Итог |
|---|---|---|
| 2. Игра в хабе, рекорд | владелец с телефона | игра работает, рекорды сохраняются; в инкогнито сбрасываются - так и задумано (D-024: гость, данные в `localStorage` хаба) |
| 4. Заголовки игры | `curl -sI https://snake.play.youranus.ru/` | CSP с `frame-ancestors https://games.youranus.ru` и только им, `Permissions-Policy`, `Cross-Origin-Opener-Policy: same-origin`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer` |
| 5. Изоляция | `curl` | `nope.play` - 404, `Service-Worker: script` - 403 |
| 8. Выкладка по пушу | `gh run list`; `curl` хаба и `phaser-2d.play…` | CI и Publish на вершине `main` (`718bb48`) - `success`; прод отдаёт `phaser-2d-1.1.0.zip`, в игре есть "Уровень" |
| 9. Откат и возврат | по `OPERATIONS.md` | владелец: "вроде все сделал" - вывода агент не видел |
| 10. Неизменяемость | CI `docker.yml` (повторный запуск `games` - "без изменений"), локальная копия набора файлов образа (сессия №3) | ✅; на проде нарочно не ломали (спецификация: не на опубликованной игре) |

## Что НЕ проверено

- Критерий 9 (откат на прошлый тег и возврат) - выводом не подтверждён.
- Полный `pnpm check` на VPS-1 не гонялся (там нет pnpm) - только CI (зелёный на `718bb48`).
- CI на `dev` с новым шагом `check-prod` - после пуша; смотреть через API GitHub (`actions/runs?branch=dev`).
- Кеш обложек на проде: не выложен (коммит в `dev`, не в `main`), заголовки на `play.youranus.ru` не смотрели;
  ускорение на телефоне - **предположение**: убирает повторные загрузки, первое открытие
  (DNS + TLS до второго хоста) - нет.
- Браузеры кроме Chromium, настоящая видеокарта; медленная загрузка обложек с телефона
  при первом открытии - не воспроизводилась (ROADMAP, "Слабые места").

## VPS

| Что | Значение |
|---|---|
| Сервер | VPS-1, x86_64 → образы `linux/amd64` |
| Веб-адрес | `2.26.198.231` (D-036) |
| Traefik | Traefik владельца; наши контейнеры - в сети `gf-edge` (D-038) |
| Сертификаты | `gf-play`: `dns01` → только `*.play.youranus.ru` (П-037); `play`, `games` - действующий `*.youranus.ru` |
| Прод-конфиг | `/etc/docker/containers/game-factory/{compose.yml,.env}`, `/usr/local/sbin/gf-update` - установлены 2026-09-25 |
| Данные | том `game-factory_gf-data`; в бэкап не входит (воспроизводим из образов) |

## Блокеры

Нет.

## Следующий шаг

1. **Роль "Безопасность" (D-045):** инструкция роли `SECURITY` - в `global-agent-kit/roles/`
   (там уже REVIEWER, TESTER, DESIGNER, ACCEPTANCE и сторож ролей), в проекте - сабагент
   `.claude/agents/security.md` поверх неё: вход - diff и `docs/SECURITY.md`, выход - находки
   с пунктом чек-листа, правок нет. Обязательна для play-server, моста, SDK, загрузки (ROLES.md).
2. **Этап 1** (ROADMAP): Postgres + Drizzle, Better Auth, сохранения и очки через API хаба,
   CSRF, серверная проверка очков - каждый шаг через роль "Безопасность".
3. Выкладка `dev` → `main` - когда скажет владелец (OPERATIONS.md). После неё проверить:
   `curl -sI 'https://play.youranus.ru/covers/snake?v=1.0.0'` → `immutable`; без `?v` →
   `no-cache` и `etag`; CI на `main` с шагом `check-prod` зелёный.

## Открытые вопросы к владельцу

- Выкладка `dev` → `main` (кеш обложек, `check-prod`, ветка `dev` в CI) - когда.
- Приёмка 🔎 "Слабых мест" в ROADMAP.

## Хэндовер: новая сессия

1. Первая строка ответа - канарейка (правило кита), нумерация ходов с 1.
2. `CLAUDE.md` ("Где работает агент"), этот файл, `docs/ROADMAP.md`, `docs/PITFALLS.md`.
3. **VPS-1:** всё про сервер; Docker и root - только блоками владельцу (П-036).
   Как устроен прод и что делать руками - `docs/OPERATIONS.md`.
4. **Облако:** сначала `git fetch`; при "forced update" - П-039, не `pull`. Работать
   от `origin/dev` (D-042); новых веток не создавать, в том числе `claude/…` от среды.
   `pnpm install && GF_CHROMIUM_PATH=/opt/pw-browsers/chromium pnpm check` -
   ожидается 49 юнит, 20 e2e. Пуш в `dev` - только CI; в `main` = выкладка на прод
   через ≤ 15 минут: красный CI не выкладывается, игра без смены `version` - тоже (П-034).
   Хук типографики меняет тире и кавычки на `-` и `"` - не спорить с ним (правило кита).
5. **Публичный репозиторий (D-039, D-043):** ничего о сервере сверх веб-адреса, домена и
   файлов `deploy/` нашего проекта; перед коммитом - проверка по списку D-043.

Контекст решений, который легко потерять:
- Игры собираются в CI, сервер только публикует (D-034); прод-конфиг - копии root (D-035).
- Веб - на отдельном адресе сервера (D-036). Репозиторий и образы публичные (D-037).
- Домен игр сейчас `*.play.youranus.ru`; новый домен - перед открытием (D-010).
- Офлайн-архив - только для статических игр (D-012). Хаб - гость без авторизации (D-024).
- Коммит - в `dev`, один на сессию, с "да" владельца; в `main` - перемоткой и отдельным "да"
  (D-042, заменило D-030). Минуты CI беречь (D-031).
- Метка содержимого (`contentTag`) - в ETag игр и обложки; на ней держится `check-prod`
  (D-044) - формат ETag не менять молча.
- Изоляция прода (D-038): хаб и сервер игр без интернета и без соседей - если хабу понадобится
  внешний API, это решение, а не правка compose.
