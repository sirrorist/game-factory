# /etc/docker/containers/game-factory/.env (root, 0644 - секретов здесь нет).
# main - выкладывать каждый зелёный коммит main.
# <полный sha коммита> - закрепить версию: откат и заморозка автообновления.
GF_IMAGE_TAG=main
